#include <stdio.h>

int gcd(int a, int b) {
	if(b==0)return a;
	return gcd(b,a%b);
}

int main() {
	int a, b;
	scanf("%d%d", &a, &b);
	int g = gcd(a, b);
	int lcm = a / g * b;
	printf("%d", lcm);
}
//递归回溯（终止后将a=x,b=0向上传递） 
