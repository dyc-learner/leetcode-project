#include <vector>
using namespace std;
class Solution {
public:
    bool isRectangleOverlap(vector<int>& rec1, vector<int>& rec2) {
      bool noOverlap=rec1[0]>=rec2[2]||rec1[1]>=rec2[3]||rec2[0]>=rec1[2]||rec2[1]>=rec1[3];
    return !noOverlap;
    }
};//正难则反思想，先考虑两个矩形不重合的情况
